<Window x:Class="PokerTournamentDirector.Views.TournamentSetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nouveau Tournoi" 
        Height="800" Width="1200"
        Background="#1a1a2e"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="StepIndicator" TargetType="Border">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="Background" Value="#0f3460"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#a0a0a0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="ComboStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="PlayerListStyle" TargetType="ListBox">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#00ff88"/>
                    <Setter Property="Foreground" Value="#1a1a2e"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1a2332"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER avec indicateur d'étapes -->
        <Border Grid.Row="0" Background="#0f3460" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                          Text="{Binding StepTitle}" 
                          FontSize="28" 
                          FontWeight="Bold" 
                          Foreground="White"
                          VerticalAlignment="Center"/>

                <!-- Indicateur d'étapes -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Étape 1 -->
                    <StackPanel Orientation="Horizontal">
                        <Border Width="40" Height="40" CornerRadius="20">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#0f3460"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStep}" Value="1">
                                            <Setter Property="Background" Value="#00ff88"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="1" FontSize="18" FontWeight="Bold" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentStep}" Value="1">
                                                <Setter Property="Foreground" Value="#1a1a2e"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                        <TextBlock Text="Config" Foreground="#a0a0a0" VerticalAlignment="Center" Margin="8,0,20,0"/>
                    </StackPanel>

                    <TextBlock Text="→" Foreground="#a0a0a0" VerticalAlignment="Center" Margin="0,0,20,0" FontSize="20"/>

                    <!-- Étape 2 -->
                    <StackPanel Orientation="Horizontal">
                        <Border Width="40" Height="40" CornerRadius="20">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#0f3460"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStep}" Value="2">
                                            <Setter Property="Background" Value="#00ff88"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="2" FontSize="18" FontWeight="Bold" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentStep}" Value="2">
                                                <Setter Property="Foreground" Value="#1a1a2e"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                        <TextBlock Text="Joueurs" Foreground="#a0a0a0" VerticalAlignment="Center" Margin="8,0,20,0"/>
                    </StackPanel>


                    <TextBlock Text="→" Foreground="#a0a0a0" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="20"/>

                    <!-- Étape 3: Tables - NOUVEAU -->
                    <StackPanel Orientation="Horizontal">
                        <Border Width="40" Height="40" CornerRadius="20">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#0f3460"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStep}" Value="3">
                                            <Setter Property="Background" Value="#00ff88"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="3" FontSize="18" FontWeight="Bold" 
                  HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentStep}" Value="3">
                                                <Setter Property="Foreground" Value="#1a1a2e"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                        <TextBlock Text="Tables" Foreground="#a0a0a0" VerticalAlignment="Center" Margin="8,0,15,0"/>
                    </StackPanel>

                    <TextBlock Text="→" Foreground="#a0a0a0" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="20"/>


                    <!-- Étape 4 -->
                    <StackPanel Orientation="Horizontal">
                        <Border Width="40" Height="40" CornerRadius="20">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#0f3460"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentStep}" Value="4">
                                            <Setter Property="Background" Value="#00ff88"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="4" FontSize="18" FontWeight="Bold" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentStep}" Value="4">
                                                <Setter Property="Foreground" Value="#1a1a2e"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                        <TextBlock Text="Lancer" Foreground="#a0a0a0" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Column="2" Content="✕" 
                       Click="CloseWindow_Click"
                       Width="40" Height="40"
                       Background="Transparent"
                       Foreground="White"
                       FontSize="24"
                       HorizontalAlignment="Right"
                       BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- CONTENU PRINCIPAL -->
        <Grid Grid.Row="1" Margin="20">
            
            <!-- ÉTAPE 1 : Configuration -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentStep}" Value="1">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Colonne gauche : Template + Infos de base -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="📝 Utiliser un modèle (optionnel)" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

                                <ComboBox ItemsSource="{Binding AvailableTemplates}"
         SelectedItem="{Binding SelectedTemplate}"
         Height="40"
         FontSize="14"
         Padding="10"
         VerticalContentAlignment="Center"
         Background="#0f3460"
         Foreground="Black"
         FontWeight="Bold"
         BorderBrush="#00ff88"
         BorderThickness="2">

                                    <!-- Template pour afficher les items -->
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}" 
                       Foreground="Black"
                       FontSize="14"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>

                                    <!-- Style pour les items dans le dropdown -->
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Background" Value="#0f3460"/>
                                            <Setter Property="Padding" Value="10"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ComboBoxItem">
                                                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#1a4a8a"/>
                                                                <!-- Changement de couleur du texte au survol -->
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#00ff88"/>
                                                                <Setter Property="Foreground" Value="#1a1a2e"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="⚙️ Configuration générale" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                                
                                <TextBlock Text="Nom du tournoi" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding TournamentName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>

                                <TextBlock Text="Buy-in (€)" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding BuyIn, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>

                                <TextBlock Text="Starting Stack" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding StartingStack, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>

                                <TextBlock Text="Structure de blinds" Style="{StaticResource LabelStyle}"/>
                                <ComboBox ItemsSource="{Binding AvailableBlindStructures}"
         SelectedItem="{Binding SelectedBlindStructure}"
         DisplayMemberPath="Name"
         Height="40"
         FontSize="14"
         Padding="10"
         VerticalContentAlignment="Center"
         Background="#0f3460"
         Foreground="Black"
         FontWeight="Bold"
         BorderBrush="#00ff88"
         BorderThickness="2">
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Background" Value="#0f3460"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="Padding" Value="10"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ComboBoxItem">
                                                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#1a4a8a"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Border" Property="Background" Value="#00ff88"/>
                                                                <Setter Property="Foreground" Value="#1a1a2e"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                </ComboBox>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Colonne droite : Options avancées -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="🪑 Tables &amp; Places" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                                
                                <TextBlock Text="Joueurs max" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding MaxPlayers, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>

                                <TextBlock Text="Places par table" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding SeatsPerTable, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>

                                <TextBlock Text="Late registration (niveaux)" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding LateRegLevels, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="🔄 Recaves" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
                                
                                <CheckBox Content="Autoriser les recaves" 
                                         IsChecked="{Binding AllowRebuys}"
                                         Foreground="White" 
                                         FontSize="16"
                                         Margin="0,0,0,15"/>

                                <StackPanel Visibility="{Binding AllowRebuys, Converter={StaticResource BoolToVisibleConverter}}">
                                    <TextBlock Text="Montant recave (€)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding RebuyAmount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>

                                    <TextBlock Text="Max recaves par joueur (0 = illimité)" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Text="{Binding MaxRebuysPerPlayer, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </ScrollViewer>

            <!-- ÉTAPE 2 : Inscription joueurs -->
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentStep}" Value="2">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Joueurs disponibles -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="👥 Joueurs disponibles" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        
                        <TextBox Grid.Row="1" 
                                Text="{Binding PlayerSearchText, UpdateSourceTrigger=PropertyChanged}"
                                Style="{StaticResource InputStyle}"
                                Margin="0,0,0,10">
                            <TextBox.Tag>🔍 Rechercher...</TextBox.Tag>
                        </TextBox>

                        <ListBox Grid.Row="2"
                                ItemsSource="{Binding AvailablePlayers}"
                                SelectedItem="{Binding SelectedAvailablePlayer}"
                                Style="{StaticResource PlayerListStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Nickname, StringFormat='@{0}'}" 
                                                  FontSize="12" Foreground="#a0a0a0"
                                                  Visibility="{Binding Nickname, Converter={StaticResource NullToCollapsedConverter}}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Button Grid.Row="3" 
                               Content="Inscrire tous →→"
                               Command="{Binding RegisterAllPlayersCommand}"
                               Height="40"
                               Background="#0f3460"
                               Foreground="White"
                               FontSize="14"
                               Margin="0,10,0,0"/>
                    </Grid>
                </Border>

                <!-- Boutons de transfert -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10">
                    <Button Content="→" 
                           Command="{Binding RegisterPlayerCommand}"
                           Width="50" Height="50"
                           Background="#00ff88"
                           Foreground="#1a1a2e"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,5"/>
                    
                    <Button Content="←" 
                           Command="{Binding UnregisterPlayerCommand}"
                           Width="50" Height="50"
                           Background="#e94560"
                           Foreground="White"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="0,5"/>
                </StackPanel>

                <!-- Joueurs inscrits -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="✅ Joueurs inscrits : " FontSize="18" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="{Binding TotalPlayers}" FontSize="18" FontWeight="Bold" Foreground="#00ff88"/>
                        </StackPanel>

                        <ListBox Grid.Row="1"
                                ItemsSource="{Binding RegisteredPlayers}"
                                SelectedItem="{Binding SelectedRegisteredPlayer}"
                                Style="{StaticResource PlayerListStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Nickname, StringFormat='@{0}'}" 
                                                  FontSize="12" Foreground="#a0a0a0"
                                                  Visibility="{Binding Nickname, Converter={StaticResource NullToCollapsedConverter}}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Border Grid.Row="2" Background="#0f3460" CornerRadius="10" Padding="15" Margin="0,10,0,0">
                            <StackPanel>
                                <TextBlock Text="{Binding TotalPrizePool, StringFormat='Prize Pool estimé: {0:C}'}" 
                                          FontSize="16" FontWeight="Bold" Foreground="#ffd700" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- ÉTAPE 3 : Gestion des tables - VERSION AMÉLIORÉE -->
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentStep}" Value="3">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20">
                        <TextBlock Text="🎲 Gestion des tables" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      Foreground="White" 
                      Margin="0,0,0,20"
                      HorizontalAlignment="Center"/>

                        <!-- Panneau de contrôle -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Placement automatique -->
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="Placement automatique" 
                                  FontSize="16" 
                                  Foreground="White" 
                                  FontWeight="Bold"
                                  Margin="0,0,0,10"/>

                                    <Button Content="🎯 Placer automatiquement"
                               Command="{Binding AutoAssignPlayersCommand}"
                               Height="45"
                               Background="#00ff88"
                               Foreground="#1a1a2e"
                               FontSize="16"
                               FontWeight="Bold"/>

                                    <TextBlock Text="Répartit tous les joueurs équitablement" 
                                  FontSize="12" 
                                  Foreground="#a0a0a0"
                                  TextWrapping="Wrap"
                                  Margin="0,5,0,0"/>
                                </StackPanel>

                                <!-- Équilibrage -->
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="Équilibrage" 
                                  FontSize="16" 
                                  Foreground="White" 
                                  FontWeight="Bold"
                                  Margin="0,0,0,10"/>

                                    <Button Content="⚖️ Équilibrer les tables"
                               Command="{Binding BalanceTablesCommand}"
                               Height="45"
                               Background="#0f3460"
                               Foreground="White"
                               FontSize="16"
                               FontWeight="Bold"/>

                                    <TextBlock Text="Corrige les déséquilibres entre tables" 
                                  FontSize="12" 
                                  Foreground="#a0a0a0"
                                  TextWrapping="Wrap"
                                  Margin="0,5,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Statut -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="📊 Statut" 
                                  FontSize="18" 
                                  Foreground="White" 
                                  FontWeight="Bold"
                                  Margin="0,0,0,10"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Tables actives : " Foreground="#a0a0a0" FontSize="14"/>
                                        <TextBlock Text="{Binding TableCount}" Foreground="#00ff88" FontSize="14" FontWeight="Bold"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Joueurs placés : " Foreground="#a0a0a0" FontSize="14"/>
                                        <TextBlock FontSize="14" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="❌ Non"/>
                                                    <Setter Property="Foreground" Value="#e94560"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AllPlayersAssigned}" Value="True">
                                                            <Setter Property="Text" Value="✅ Oui"/>
                                                            <Setter Property="Foreground" Value="#00ff88"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Équilibre : " Foreground="#a0a0a0" FontSize="14"/>
                                        <TextBlock Text="{Binding BalanceStatus}" FontSize="14" FontWeight="Bold" Foreground="White"/>
                                    </StackPanel>
                                </StackPanel>

                                <Button Grid.Column="1"
                           Content="🔄"
                           Command="{Binding RefreshTablesCommand}"
                           Width="50" Height="50"
                           Background="#0f3460"
                           Foreground="White"
                           FontSize="20"
                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Info drag & drop -->
                        <Border Background="#16213e" 
                   BorderBrush="#00ff88" 
                   BorderThickness="2" 
                   CornerRadius="10" 
                   Padding="15" 
                   Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="💡 Gestion manuelle" 
                              FontSize="16" 
                              FontWeight="Bold" 
                              Foreground="#00ff88" 
                              Margin="0,0,0,10"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#a0a0a0" FontSize="13">
                        • <Bold>Clic droit</Bold> sur un joueur → menu contextuel (déplacer, verrouiller)
                                </TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#a0a0a0" FontSize="13" Margin="0,5,0,0">
                        • <Bold>🔒 Verrouiller</Bold> un joueur l'empêche d'être déplacé par l'équilibrage automatique
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Liste des tables -->
                        <ItemsControl ItemsSource="{Binding TableLayouts}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardStyle}" Width="320" Margin="5">
                                        <StackPanel>
                                            <!-- Header de la table -->
                                            <Border Background="#0f3460" CornerRadius="8" Padding="10" Margin="0,0,0,10">
                                                <Grid>
                                                    <TextBlock Text="{Binding TableNumber, StringFormat='TABLE {0}'}" 
                                                  FontSize="20" 
                                                  FontWeight="Bold" 
                                                  Foreground="#00ff88"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>

                                            <!-- Compteur de joueurs -->
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                                                <TextBlock Text="{Binding PlayerCount}" 
                                              FontSize="28" 
                                              FontWeight="Bold" 
                                              Foreground="White"/>
                                                <TextBlock Text="{Binding MaxSeats, StringFormat=' / {0}'}" 
                                              FontSize="18" 
                                              Foreground="#a0a0a0"
                                              VerticalAlignment="Bottom"
                                              Margin="5,0,0,3"/>
                                                <TextBlock Text=" joueurs" 
                                              FontSize="14" 
                                              Foreground="#a0a0a0"
                                              VerticalAlignment="Bottom"
                                              Margin="3,0,0,3"/>
                                            </StackPanel>

                                            <!-- Sièges -->
                                            <ItemsControl ItemsSource="{Binding Seats}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="#0f3460" 
                                                   CornerRadius="6" 
                                                   Padding="10,8" 
                                                   Margin="0,3"
                                                   MouseRightButtonUp="Seat_RightClick"
                                                   Tag="{Binding}">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                                                            <Setter Property="Background" Value="#1a3a52"/>
                                                                            <Setter Property="Cursor" Value="Hand"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding IsLocked}" Value="True">
                                                                            <Setter Property="BorderBrush" Value="#ffd700"/>
                                                                            <Setter Property="BorderThickness" Value="2"/>
                                                                        </DataTrigger>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#2a4a62"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>

                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="35"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>

                                                                <TextBlock Grid.Column="0" 
                                                              Text="{Binding SeatNumber, StringFormat='#{0}'}" 
                                                              Foreground="#00ff88" 
                                                              FontSize="13"
                                                              FontWeight="Bold"
                                                              VerticalAlignment="Center"/>

                                                                <TextBlock Grid.Column="1" 
                                                              Text="{Binding PlayerName}" 
                                                              Foreground="White" 
                                                              FontSize="14"
                                                              FontWeight="Bold"
                                                              VerticalAlignment="Center"
                                                              Margin="5,0">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsOccupied}" Value="False">
                                                                                    <Setter Property="Text" Value="(vide)"/>
                                                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                                                    <Setter Property="Foreground" Value="#505050"/>
                                                                                    <Setter Property="FontWeight" Value="Normal"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>

                                                                <TextBlock Grid.Column="2"
                                                              Text="🔒"
                                                              FontSize="14"
                                                              VerticalAlignment="Center"
                                                              Visibility="{Binding IsLocked, Converter={StaticResource BoolToVisConverter}}"/>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- ÉTAPE 4 : Résumé et lancement -->
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentStep}" Value="4">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Border Style="{StaticResource CardStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="500">
                    <StackPanel>
                        <TextBlock Text="🎯 Résumé du tournoi" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Nom:" Foreground="#a0a0a0" FontSize="16" Margin="0,5"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding TournamentName}" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,5"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Joueurs inscrits:" Foreground="#a0a0a0" FontSize="16" Margin="0,5"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding TotalPlayers}" Foreground="#00ff88" FontSize="16" FontWeight="Bold" Margin="0,5"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Buy-in:" Foreground="#a0a0a0" FontSize="16" Margin="0,5"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding BuyIn, StringFormat='{}{0:C}'}" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,5"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Prize Pool:" Foreground="#a0a0a0" FontSize="16" Margin="0,5"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding TotalPrizePool, StringFormat='{}{0:C}'}" Foreground="#ffd700" FontSize="16" FontWeight="Bold" Margin="0,5"/>
                        </Grid>

                        <Button Content="🚀 DÉMARRER LE TOURNOI"
                               Command="{Binding StartTournamentCommand}"
                               Height="60"
                               Background="#00ff88"
                               Foreground="#1a1a2e"
                               FontSize="22"
                               FontWeight="Bold"
                               Margin="0,20,0,0"
                               IsEnabled="{Binding CanStartTournament}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- FOOTER avec boutons navigation -->
        <Border Grid.Row="2" Background="#0f3460" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                       Content="← Retour"
                       Command="{Binding PreviousStepCommand}"
                       HorizontalAlignment="Left"
                       Width="150" Height="45"
                       Background="#e94560"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Bold"
                       Visibility="{Binding CurrentStep, Converter={StaticResource ZeroToCollapsedConverter}}"/>

                <Button Grid.Column="1"
                       Content="Annuler le tournoi"
                       Command="{Binding CancelTournamentCommand}"
                       Width="180" Height="45"
                       Background="#16213e"
                       Foreground="#e94560"
                       FontSize="14"/>

                <Button Grid.Column="2" 
                       Content="Suivant →"
                       Command="{Binding NextStepCommand}"
                       HorizontalAlignment="Right"
                       Width="150" Height="45"
                       Background="#00ff88"
                       Foreground="#1a1a2e"
                       FontSize="16"
                       FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentStep}" Value="4">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
