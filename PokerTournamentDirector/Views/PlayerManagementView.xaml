<Window x:Class="PokerTournamentDirector.Views.PlayerManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PokerTournamentDirector.ViewModels"
        Title="Gestion des Joueurs" 
        Height="800" Width="1400"
        Background="#1a1a2e"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#a0a0a0"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#00ff88"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="RowBackground" Value="#16213e"/>
            <Setter Property="AlternatingRowBackground" Value="#1a1a2e"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="#00ff88"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Height" Value="40"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#00ff88"/>
                    <Setter Property="Foreground" Value="#1a1a2e"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0f3460"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LISTE DES JOUEURS -->
        <Border Grid.Column="0" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0">
                    <TextBlock Text="👥 Base de Joueurs" Style="{StaticResource TitleStyle}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding ActivePlayers}" FontSize="20" FontWeight="Bold" Foreground="#00ff88" VerticalAlignment="Center"/>
                        <TextBlock Text=" actifs / " FontSize="16" Foreground="#a0a0a0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalPlayers}" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text=" total" FontSize="16" Foreground="#a0a0a0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- TOOLBAR -->
                <Grid Grid.Row="1" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- RECHERCHE -->
                    <Grid Grid.Column="0">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                Style="{StaticResource TextBoxStyle}"
                                Margin="0,0,10,0">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        <TextBlock Text="🔍 Rechercher..." 
                                  IsHitTestVisible="False"
                                  Foreground="#666"
                                  FontSize="14"
                                  Margin="15,0,0,0"
                                  VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SearchText}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>

                    <!-- FILTRES -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <CheckBox Content="Actifs uniquement" 
                                 IsChecked="{Binding ShowActiveOnly}"
                                 Foreground="White"
                                 VerticalAlignment="Center"
                                 Margin="0,0,15,0"/>
                        <Button Content="🔄 Actualiser" 
                               Command="{Binding LoadPlayersCommand}"
                               Width="120" Height="40"
                               Background="#0f3460" 
                               Foreground="White"/>
                    </StackPanel>
                </Grid>

                <!-- DATAGRID -->
                <DataGrid Grid.Row="2" 
                         ItemsSource="{Binding Players}"
                         SelectedItem="{Binding SelectedPlayer}"
                         Style="{StaticResource DataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="200"/>
                        <DataGridTextColumn Header="Pseudo" Binding="{Binding Nickname}" Width="150"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                        <DataGridTextColumn Header="Téléphone" Binding="{Binding Phone}" Width="130"/>
                        <DataGridTextColumn Header="Tournois" Binding="{Binding TotalTournamentsPlayed}" Width="80"/>
                        <DataGridTextColumn Header="Victoires" Binding="{Binding TotalWins}" Width="80"/>
                        <DataGridTextColumn Header="ITM" Binding="{Binding TotalITM}" Width="70"/>
                        <DataGridTextColumn Header="Gains (€)" Binding="{Binding TotalWinnings, StringFormat='{}{0:N0}'}" Width="100"/>
                        <DataGridTemplateColumn Header="Statut" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToActiveConverter}}"
                                              Foreground="{Binding IsActive, Converter={StaticResource BoolToActiveColorConverter}}"
                                              FontWeight="Bold"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- ACTIONS BOUTONS -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="➕ Nouveau" 
                           Command="{Binding NewPlayerCommand}"
                           Width="140" Height="50"
                           Background="#00ff88" 
                           Foreground="#1a1a2e"
                           FontSize="16"
                           FontWeight="Bold"
                           Margin="5"/>

                    <Button Content="✏️ Modifier" 
                           Command="{Binding EditPlayerCommand}"
                           Width="140" Height="50"
                           Background="#0f3460" 
                           Foreground="White"
                           FontSize="16"
                           Margin="5"
                           IsEnabled="{Binding SelectedPlayer, Converter={StaticResource NullToBoolConverter}}"/>

                    <Button Content="🗑️ Désactiver" 
                           Command="{Binding DeletePlayerCommand}"
                           Width="140" Height="50"
                           Background="#e94560" 
                           Foreground="White"
                           FontSize="16"
                           Margin="5"
                           IsEnabled="{Binding SelectedPlayer, Converter={StaticResource NullToBoolConverter}}"/>

                    <Button Content="📥 Importer CSV" 
                           Command="{Binding ImportCsvCommand}"
                           Width="150" Height="50"
                           Background="#ffd700" 
                           Foreground="#1a1a2e"
                           FontSize="16"
                           Margin="5"/>

                    <Button Content="📤 Exporter CSV" 
                           Command="{Binding ExportCsvCommand}"
                           Width="150" Height="50"
                           Background="#ffd700" 
                           Foreground="#1a1a2e"
                           FontSize="16"
                           Margin="5"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- FORMULAIRE ÉDITION -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}"
               Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibleConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- TITRE -->
                <TextBlock Grid.Row="0" Style="{StaticResource TitleStyle}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}✏️ {0}">
                            <Binding Path="SelectedPlayer" Converter="{StaticResource PlayerEditTitleConverter}"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>

                <!-- CHAMPS -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Nom *" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding EditName}" Style="{StaticResource TextBoxStyle}"/>

                        <TextBlock Text="Pseudo" Style="{StaticResource LabelStyle}" Margin="0,15,0,5"/>
                        <TextBox Text="{Binding EditNickname}" Style="{StaticResource TextBoxStyle}"/>

                        <TextBlock Text="Email" Style="{StaticResource LabelStyle}" Margin="0,15,0,5"/>
                        <TextBox Text="{Binding EditEmail}" Style="{StaticResource TextBoxStyle}"/>

                        <TextBlock Text="Téléphone" Style="{StaticResource LabelStyle}" Margin="0,15,0,5"/>
                        <TextBox Text="{Binding EditPhone}" Style="{StaticResource TextBoxStyle}"/>

                        <TextBlock Text="Notes" Style="{StaticResource LabelStyle}" Margin="0,15,0,5"/>
                        <TextBox Text="{Binding EditNotes}" 
                                Style="{StaticResource TextBoxStyle}"
                                Height="120"
                                TextWrapping="Wrap"
                                AcceptsReturn="True"
                                VerticalScrollBarVisibility="Auto"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- BOUTONS ACTION -->
                <StackPanel Grid.Row="2" Margin="0,20,0,0">
                    <Button Content="💾 Enregistrer" 
                           Command="{Binding SavePlayerCommand}"
                           Height="50"
                           Background="#00ff88" 
                           Foreground="#1a1a2e"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                    <Button Content="❌ Annuler" 
                           Command="{Binding CancelEditCommand}"
                           Height="45"
                           Background="#e94560" 
                           Foreground="White"
                           FontSize="16"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- MESSAGE SI PAS D'ÉDITION -->
        <Border Grid.Column="1" Style="{StaticResource CardStyle}"
               Visibility="{Binding IsEditing, Converter={StaticResource BoolToCollapsedConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="👤" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="Sélectionnez un joueur" 
                          FontSize="18" 
                          Foreground="#a0a0a0"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>
                <TextBlock Text="ou créez-en un nouveau" 
                          FontSize="14" 
                          Foreground="#666"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>