<Window x:Class="PokerTournamentDirector.Views.QuickTournamentLaunchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🚀 Lancer Tournoi Rapide"
        Width="900" Height="700"
        Background="#1a1a2e"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🚀 Lancer Tournoi Rapide" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#00ff88"/>
            <TextBlock Text="{Binding SelectedChampionship.Name}" 
                       FontSize="16" 
                       Foreground="#a0a0a0"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Nom tournoi -->
        <Border Grid.Row="1" 
                Background="#16213e" 
                CornerRadius="10" 
                Padding="15"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Nom de la manche" 
                           FontSize="14" 
                           Foreground="#a0a0a0"
                           Margin="0,0,0,5"/>
                <TextBox Text="{Binding TournamentName, UpdateSourceTrigger=PropertyChanged}"
                         Background="#0f3460"
                         Foreground="White"
                         BorderBrush="#00ff88"
                         BorderThickness="2"
                         Padding="10"
                         FontSize="16"
                         Height="40"/>
            </StackPanel>
        </Border>

        <!-- Inscription joueurs -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Joueurs disponibles -->
            <Border Grid.Column="0" Background="#16213e" CornerRadius="10" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Joueurs disponibles" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="White"/>

                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding AvailablePlayers}"
                             SelectionMode="Multiple"
                             Background="Transparent"
                             BorderThickness="0"
                             Margin="0,10,0,0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}"
                                          IsChecked="{Binding IsSelected}"
                                          Foreground="White"
                                          FontSize="14"
                                          Margin="0,5"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Boutons milieu -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Content="➡️" 
                        Command="{Binding RegisterSelectedPlayersCommand}"
                        Width="60" Height="60"
                        FontSize="24"
                        Background="#00ff88"
                        Foreground="#1a1a2e"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,0,20"/>
                <Button Content="⬅️" 
                        Command="{Binding UnregisterSelectedPlayersCommand}"
                        Width="60" Height="60"
                        FontSize="24"
                        Background="#e94560"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>

            <!-- Joueurs inscrits -->
            <Border Grid.Column="2" Background="#16213e" CornerRadius="10" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Text="Joueurs inscrits" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Foreground="#00ff88"/>
                        <TextBlock FontSize="13" Foreground="#a0a0a0" Margin="0,3,0,0">
                            <Run Text="{Binding RegisteredPlayers.Count, Mode=OneWay}"/>
                            <Run Text=" joueur(s)"/>
                        </TextBlock>
                    </StackPanel>

                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding RegisteredPlayers}"
                             SelectionMode="Multiple"
                             Background="Transparent"
                             BorderThickness="0"
                             Margin="0,10,0,0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}"
                                          IsChecked="{Binding IsSelected}"
                                          Foreground="White"
                                          FontSize="14"
                                          Margin="0,5"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="❌ Annuler" 
                    Click="Cancel_Click"
                    Width="120" Height="50"
                    Background="#e94560"
                    Foreground="White"
                    FontSize="16"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="0,0,10,0"/>
            <Button Content="🚀 LANCER !" 
                    Click="Launch_Click"
                    Width="180" Height="50"
                    Background="#00ff88"
                    Foreground="#0a192f"
                    FontWeight="Bold"
                    FontSize="18"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>