<Application x:Class="PokerTournamentDirector.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:PokerTournamentDirector.Converters">
    <Application.Resources>
        <!-- Converters -->
        <converters:BoolToVisibleConverter x:Key="BoolToVisibleConverter"/>
        <converters:BoolToCollapsedConverter x:Key="BoolToCollapsedConverter"/>
        <converters:ZeroToCollapsedConverter x:Key="ZeroToCollapsedConverter"/>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        <converters:BoolToActiveConverter x:Key="BoolToActiveConverter"/>
        <converters:BoolToActiveColorConverter x:Key="BoolToActiveColorConverter"/>
        <converters:PlayerEditTitleConverter x:Key="PlayerEditTitleConverter"/>
        <converters:NullToCollapsedConverter x:Key="NullToCollapsedConverter"/>
        <converters:MinutesToHoursConverter x:Key="MinutesToHoursConverter"/>
        <converters:HexToColorConverter x:Key="HexToColorConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        <converters:BoolToHistoryTextConverter x:Key="BoolToHistoryTextConverter"/>
        <converters:NotNullToBoolConverter x:Key="NotNullToBoolConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <converters:PaymentStatusToIconConverter x:Key="PaymentStatusToIconConverter"/>
        <converters:PaymentStatusToColorConverter x:Key="PaymentStatusToColorConverter"/>
        <converters:PlayerStatusConverter x:Key="PlayerStatusConverter"/>
        <converters:PlayerStatusColorConverter x:Key="PlayerStatusColorConverter"/>
        <converters:PlayerAlertConverter x:Key="PlayerAlertConverter"/>
        <converters:PlayerHasAlertConverter x:Key="PlayerHasAlertConverter"/>
        <converters:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        <converters:ProrataModeToIndexConverter x:Key="ProrataModeToIndexConverter"/>
        <converters:IntGreaterThanZeroConverter x:Key="IntGreaterThanZeroConverter"/>
        <converters:NullToVisibleConverter x:Key="NullToVisibleConverter"/>
        <converters:KillCountToEmojiConverter x:Key="KillCountToEmojiConverter"/>
        <converters:KillCountToColorConverter x:Key="KillCountToColorConverter"/>
        <converters:MultiplyBy2Converter x:Key="MultiplyBy2Converter"/>
        <converters:StartingStackToBBConverter x:Key="StartingStackToBBConverter"/>
        
        <!-- Easing doux et naturel pour les animations -->
        <SineEase x:Key="SineEaseInOut" EasingMode="EaseInOut"/>
        <QuadraticEase x:Key="QuadraticEaseInOut" EasingMode="EaseInOut"/>
        <CubicEase x:Key="CubicEaseInOut" EasingMode="EaseInOut"/>
        <DropShadowEffect x:Key="CardShadow"
                  Color="#000000"
                  Direction="315"
                  ShadowDepth="2"
                  BlurRadius="30"
                  Opacity="0.25"/>

        <!-- Styles globaux pour boutons -->
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="10"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Style CheckBox -->
                <Style TargetType="CheckBox">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Margin" Value="0,12,0,12"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CheckBox">
                                <Grid>
                                    <!-- Fond principal (comme ton Border) -->
                                    <Border x:Name="CheckBorder"
                                    Background="Transparent"
                                    BorderBrush="#0f3460"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="6"
                                    Effect="{StaticResource CardShadow}">

                                        <!-- Contenu : coche + texte -->
                                        <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left">

                                            <!-- Zone de la coche -->
                                            <Border x:Name="CheckMarkBorder"
                                            Width="22"
                                            Height="22"
                                            Background="#0f3460"
                                            BorderBrush="#00ff88"
                                            BorderThickness="1"
                                            CornerRadius="5"
                                            Margin="0,0,10,0">
                                                <!-- La coche (visible quand coché) -->
                                                <Path x:Name="CheckMark"
                                              Data="M 3 8 L 8 13 L 18 3"
                                              Stroke="#00ff88"
                                              StrokeThickness="3"
                                              StrokeEndLineCap="Round"
                                              StrokeStartLineCap="Round"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Opacity="0"/>
                                            </Border>

                                            <!-- Texte -->
                                            <ContentPresenter x:Name="ContentSite"
                                                      ContentSource="Content"
                                                      Margin="0,0,0,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      RecognizesAccessKey="True"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>

                                <!-- Triggers -->
                                <ControlTemplate.Triggers>

                                    <!-- Checked -->
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="CheckMark" Property="Opacity" Value="1"/>
                                        <Setter TargetName="CheckMarkBorder" Property="Background" Value="#0f3460"/>
                                        <Setter TargetName="CheckMarkBorder" Property="BorderBrush" Value="#00ff88"/>
                                        <Setter TargetName="CheckMarkBorder" Property="BorderThickness" Value="2"/>
                                    </Trigger>

                                    <!-- MouseOver -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="CheckBorder" Property="Background" Value="#1e2a44"/>
                                        <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#00ffaa"/>
                                        <Setter TargetName="CheckBorder" Property="BorderThickness" Value="2"/>
                                    </Trigger>

                                    <!-- Checked + MouseOver -->
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsChecked" Value="True"/>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="CheckMarkBorder" Property="Background" Value="#1e2a44"/>
                                        <Setter TargetName="CheckMarkBorder" Property="BorderBrush" Value="#00ffaa"/>
                                    </MultiTrigger>

                                    <!-- Disabled -->
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="CheckBorder" Property="Opacity" Value="0.5"/>
                                        <Setter TargetName="ContentSite" Property="Opacity" Value="0.5"/>
                                    </Trigger>

                                    <!-- Focused -->
                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#00ff88"/>
                                        <Setter TargetName="CheckBorder" Property="BorderThickness" Value="2"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
        
        <!-- Style TextBox -->
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#0f3460" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#00ff88" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                         Background="{TemplateBinding Background}"
                         BorderBrush="{TemplateBinding BorderBrush}"
                         BorderThickness="{TemplateBinding BorderThickness}"
                         CornerRadius="8"
                         SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                   Focusable="False"
                                   HorizontalScrollBarVisibility="Hidden"
                                   VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00ff88" />
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00ff88" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Style ComboBox -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <!-- Fond et couleurs cohérents avec ton thème poker -->
            <Setter Property="Background" Value="#0f3460" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#00ff88" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="FontSize" Value="15" />
            <!-- Même taille que les TextBox -->

            <!-- Ce qui corrige le texte écrasé -->
            <Setter Property="Padding" Value="12,10" />
            <!-- Espace intérieur généreux -->
            <Setter Property="MinHeight" Value="38" />
            <!-- Hauteur minimale confortable -->
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <!-- Texte centré verticalement -->

            <!-- Coins arrondis + template complet -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                   BorderBrush="{TemplateBinding BorderBrush}"
                   BorderThickness="{TemplateBinding BorderThickness}"
                   Background="{TemplateBinding Background}"
                   Focusable="False"
                   IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                   ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate>
                                        <Border x:Name="Bd"
                         Background="{TemplateBinding Background}"
                         BorderBrush="{TemplateBinding BorderBrush}"
                         BorderThickness="{TemplateBinding BorderThickness}"
                         CornerRadius="10"
                         SnapsToDevicePixels="True">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left"
                                           Margin="12,0,0,0" />
                                                <!-- Flèche du dropdown -->
                                                <Path Grid.Column="1"
                               Data="M0,0 L4,4 L8,0 Z"
                               Fill="White"
                               Margin="0,0,12,0"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <!-- Effet focus -->
                                            <Trigger Property="IsFocused" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00ff88" />
                                                <Setter TargetName="Bd" Property="BorderThickness" Value="3" />
                                                <Setter TargetName="Bd" Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect Color="#00ff88" Direction="320" ShadowDepth="0" BlurRadius="8" Opacity="0.6" />
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                            <!-- Survol -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="BorderBrush" Value="#00ff88" />
                                            </Trigger>
                                            <!-- Désactivé -->
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.5" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                       IsHitTestVisible="False"
                       Content="{TemplateBinding SelectionBoxItem}"
                       ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                       ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                       Margin="12,0,30,0"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left" />

                            <Popup x:Name="Popup"
            Placement="Bottom"
            IsOpen="{TemplateBinding IsDropDownOpen}"
            AllowsTransparency="True"
            Focusable="False"
            PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
               SnapsToDevicePixels="True"
               MinWidth="{TemplateBinding ActualWidth}"
               MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                     Background="#0f3460"
                     BorderBrush="#00ff88"
                     BorderThickness="2"
                     CornerRadius="10">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <!-- Triggers au niveau du template principal pour le ComboBox -->
                        <ControlTemplate.Triggers>
                            <!-- Désactivé pour tout le ComboBox -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>